<section class="hero">
  <div class="hero-content">
    <p class="hero-kicker">Tus plantas merecen un amigo</p>
    <h1 class="hero-title">
      Un cuaderno botánico<br />para tus plantas
    </h1>
    <p class="hero-description">
      Brote te ayuda a cuidar tus plantas con un diario personal, recordatorios inteligentes
      y una asistente de IA que responde tus dudas de jardinería. En silencio.
    </p>
    <div class="hero-actions">
      <a href="#download" class="btn btn-primary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
          <polyline points="7 10 12 15 17 10"/>
          <line x1="12" y1="15" x2="12" y2="3"/>
        </svg>
        Descargar para Android
      </a>
      <a href="/#guide" class="btn btn-secondary">Conocer más</a>
    </div>
  </div>
  <div class="hero-illustration" aria-hidden="true">
    <img
      src="/images/illustrations/A%20healthy%20Monstera%20deliciosa%20in%20a%20simple%20matte%20ceramic%20pot.png"
      alt=""
      class="hero-img"
    />
  </div>
</section>

<style>
  .hero {
    max-width: 1100px;
    margin: 0 auto;
    padding: 80px 24px 64px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: center;
    min-height: calc(100vh - 140px);
  }

  .hero-kicker {
    font-size: 0.9rem;
    font-weight: 500;
    color: #6E8E6A;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 16px;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 3.8rem);
    font-weight: 700;
    color: #3F3A36;
    line-height: 1.1;
    margin-bottom: 24px;
  }

  .hero-description {
    font-size: 1.15rem;
    color: #7B756E;
    max-width: 480px;
    line-height: 1.7;
    margin-bottom: 36px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 28px;
    border-radius: 24px;
    font-size: 1rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-primary {
    background-color: #6E8E6A;
    color: #F8F6F2;
  }

  .btn-primary:hover {
    background-color: #5A7A56;
    color: #F8F6F2;
    transform: translateY(-1px);
    box-shadow: 0 4px 16px rgba(110, 142, 106, 0.3);
  }

  .btn-secondary {
    background-color: #F3F1EC;
    color: #3F3A36;
    border: 1px solid #D4CFC6;
  }

  .btn-secondary:hover {
    background-color: #EBE7E0;
    color: #3F3A36;
  }

  .hero-illustration {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-img {
    width: 100%;
    max-width: 420px;
    height: auto;
    border-radius: 20px;
  }

  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 48px 24px 40px;
      min-height: auto;
    }

    .hero-description {
      max-width: 100%;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-illustration {
      order: -1;
      margin-bottom: -10px;
    }

    .hero-img {
      max-width: 280px;
    }
  }
</style>
